import { View, Text } from "react-native";
import React from "react";
import { Stack } from "expo-router";

const SearchLayout = () => {
  return <Stack initialRouteName="pickup"></Stack>;
};

export default SearchLayout;
